import './styles.css';

export default function Header() {
    return (
        <header className="remote-header">
            <h1>My Gallery</h1>
            <span>Loaded from header-app</span>
        </header>
    );
}
